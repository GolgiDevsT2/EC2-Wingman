/* IS_AUTOGENERATED_SO_ALLOW_AUTODELETE=YES */
/* The previous line is to allow auto deletion */

package io.golgi.wingman.gen;

import java.util.Hashtable;
import java.util.ArrayList;
import java.util.Iterator;
import com.openmindnetworks.golgi.JavaType;
import com.openmindnetworks.golgi.GolgiPayload;
import com.openmindnetworks.golgi.B64;
import com.openmindnetworks.golgi.api.GolgiException;
import com.openmindnetworks.golgi.api.GolgiAPI;
import android.os.Parcel;
import android.os.Parcelable;

public class Wingman_raiseStatusCheckAlarm_reqArg implements Parcelable
{

    public int describeContents(){
        return 0;
    }

    public void writeToParcel(Parcel dest, int flags){
        dest.writeString(this.serialise().toString());
    }

    public static final Parcelable.Creator<Wingman_raiseStatusCheckAlarm_reqArg> CREATOR = new Parcelable.Creator<Wingman_raiseStatusCheckAlarm_reqArg>() {
        public Wingman_raiseStatusCheckAlarm_reqArg createFromParcel(Parcel in) {
            return new Wingman_raiseStatusCheckAlarm_reqArg(in);
        }
        public Wingman_raiseStatusCheckAlarm_reqArg[] newArray(int size) {
            return new Wingman_raiseStatusCheckAlarm_reqArg[size];
        }
    };
    private Wingman_raiseStatusCheckAlarm_reqArg(Parcel in) {
        this(in.readString());
    }

    private boolean corrupt = false;

    public boolean isCorrupt() {
        return corrupt;
    }

    public void setCorrupt() {
        corrupt = true;
    }

    private boolean instNameIsSet = false;
    private String instName;

    public String getInstName(){
        return instName;
    }
    public boolean instNameIsSet(){
        return instNameIsSet;
    }
    public void setInstName(String instName){
        this.instName = instName;
        this.instNameIsSet = true;
    }

    public StringBuffer serialise(){
        return serialise(null);
    }

    public StringBuffer serialise(StringBuffer sb){
        return serialise("", sb);
    }

    public StringBuffer serialise(String prefix, StringBuffer sb){
        if(sb == null){
            sb = new StringBuffer();
        }

        if(this.instNameIsSet){
            sb.append(prefix + "1: " + JavaType.encodeString(this.instName) + "\n");
        }

        return sb;
    }

    public void deserialise(String str){
        deserialise(JavaType.createPayload(str));
    }

    private void deserialise(GolgiPayload payload){
        if(!isCorrupt() && payload.containsFieldKey("1:")){
            String val = payload.getField("1:");
            String str = JavaType.decodeString(val);
            if(str != null){
                setInstName(str);
            }
            else{
                setCorrupt();
            }
        }
        else{
            setCorrupt();
        }
    }

    public Wingman_raiseStatusCheckAlarm_reqArg(){
        this(true);
    }

    public Wingman_raiseStatusCheckAlarm_reqArg(boolean isSetDefault){
        super();
        instNameIsSet = isSetDefault;
        instName = new String();
    }

    public Wingman_raiseStatusCheckAlarm_reqArg(GolgiPayload payload){
        this(false);
        deserialise(payload);
    }

    public Wingman_raiseStatusCheckAlarm_reqArg(String payload){
        this(JavaType.createPayload(payload));
    }

}
